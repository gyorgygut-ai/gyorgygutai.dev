{
  "name": "gyorgygutai-dev",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run build --workspace=packages/processor && npm run test --workspaces --if-present",
    "test:watch": "npm run test:watch --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present",
    "bundleVaultIntoWorker": "npm run bundleVaultIntoWorker --workspace=packages/worker",
    "dev": "npm run build --workspace=packages/processor && npm run dev --workspace=packages/worker",
    "deploy": "npm run build --workspace=packages/processor && npm run deploy --workspace=packages/worker"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}